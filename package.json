{
  "name": "cft_web",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -r images dist/images && cp *.png dist/ 2>/dev/null || true",
    "start": "npx serve dist"
  }
}